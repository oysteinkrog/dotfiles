# AUDIT_SUMMARY.md — <Project Name> — <YYYY-MM-DD>

Generated by the rust-unsafe-code-exorcist skill at <YYYY-MM-DD HH:MM UTC>.

The single line the user pastes into their release notes / PR description.

---

## Tally

Total unsafe sites: <N>

- **(A) STRICTLY_UNAVOIDABLE:** <a> (all with falsifiable justification, hardened SAFETY comment, lint coverage)
- **(B) PERF_ONLY:** <b> (all with `safe-only` feature flag + measured per-target perf)
- **(C) REFACTORABLE:** <c> (all with property-based equivalence proof + miri-clean rewrite)

Pre-existing-UB beads filed (separately from refactor scope): <p>

---

## Soundness surface

<s> `pub` API paths reach unsafe.

- <h> have hardened SAFETY comments naming the proof obligation.
- <l> have a clippy lint catching caller-side violations.
- <r> are auto-derive-safe AFTER (C) refactor lands.

---

## Convergence

| Phase | Passes to converge | Final flip ratio | (A)→(C) flips |
|-------|--------------------|------------------|---------------|
| Phase 4 (classify)  | <p4> | <%>  | 0 |
| Phase 6 (adversarial) | <p6> | <%> | 0 |

(Flip ratio = sites changed bucket / total sites; <5% AND zero (A)→(C) = converged.)

---

## Verification harness (verify.sh)

Result: <GREEN | PARTIAL — see notes>

| Tool | Result | Notes |
|------|--------|-------|
| `cargo +nightly miri test` (stacked borrows) | GREEN | <coverage notes> |
| `cargo +nightly miri test -Zmiri-strict-provenance` | GREEN | <coverage notes> |
| `cargo +nightly miri test -Zmiri-tree-borrows` | GREEN | <coverage notes> |
| `cargo +nightly careful test` | GREEN |  |
| loom (concurrency models) | GREEN | <model count, preemption_bound> |
| cargo-fuzz (60s/target) | GREEN | <target count> |
| cargo-mutants | <coverage %> | <coverage thresholds met> |
| cargo +nightly geiger | delta: <delta> | <baseline vs post> |
| cargo test (default features) | GREEN |  |
| cargo test --features safe-only | GREEN |  |
| kani (formal verification, where applicable) | <result> | <proofs verified> |

---

## Beads

- <N> implementation beads filed.
- <K> parent epic beads (one per refactor cluster).
- <P> pre-existing-ub beads filed separately.
- Bead graph: no cycles; `br ready` shows <unblocked> bead(s) at the start.

---

## Reviewer (Phase 10 maintainer-empathy)

- **Confidence:** <High | Medium | Low>
- **Concerns filed:** <N>; of those, <addressed> addressed in revised plans, <deferred> deferred (with follow-up beads).

Filed at: `<audit-dir>/REVIEWER_RESPONSES.md`.

---

## Triangulation (multi-model on high-risk sites)

- Top-<N> sites verified across <Claude | Codex | Gemini | Grok> (whichever available).
- Dissents resolved: <count>.

---

## Public-API impact

- Total `pub` items changed: <count>
- Non-breaking: <count>
- Breaking-but-trivial (`#[deprecated]` shims provided): <count>
- Breaking-and-deep (MIGRATION.md entry per item): <count>

Recommended version bump: **v<X.Y.Z+ or Y+1.0 or X+1.0.0>**

`cargo public-api` diff: <link to file>
`cargo semver-checks`: <PASS | FAIL>

---

## Dependency soundness

- Deps with `cargo geiger > 0`: <count>
- Deps with reachable-unsafe from our pub API: <count>
- Mitigations:
  - WRAPPED: <count>
  - REPLACED: <count>
  - UPSTREAM issues filed: <count>
  - JUSTIFIED in our docs: <count>

Dep-version lockfile: <link>; cargo-vet certifications: <published | not yet>.

---

## Single-line summary (for release notes / PR title)

Audit of <project> v<X.Y.Z> by rust-unsafe-code-exorcist complete:
A=<a>, B=<b>, C=<c>, pre-existing-ub=<p>, verify.sh=GREEN, reviewer-confidence=<H/M/L>.

---

## Next steps (the user's decision points)

1. **Review the authorized diffs / ordinary branches / PRs** at <list of commit hashes, branch names, or PR URLs>.
2. **Land in dependency order** (parents first; the bead graph shows the order).
3. **After the selected changes land**, run `bash <audit-dir>/verify.sh` against the merged main.
4. **Cut the release** with the changelog entry from `<audit-dir>/audit/changelog-drafts/CHANGELOG-entry.md`.
5. **File the security advisory** (if applicable) from `<audit-dir>/audit/changelog-drafts/RUSTSEC-DRAFT.toml`.
6. **Address pre-existing-ub-N beads** at your own pace (separate refactor cycles).
7. **Schedule the next audit** based on the project's release cadence + observed drift.

---

## Audit artifacts

- `<audit-dir>/unsafe-inventory.jsonl` — full inventory
- `<audit-dir>/audit/sites/` — per-site write-ups
- `<audit-dir>/audit/synthesis/` — invariants, soundness surface, refactor clusters
- `<audit-dir>/audit/classification/` — per-site (A)/(B)/(C) with justification
- `<audit-dir>/audit/plans/` — per-site refactor plans
- `<audit-dir>/audit/tests/` — equivalence + regression tests
- `<audit-dir>/audit/phase7/verification-log.md` — verbatim tool output
- `<audit-dir>/.beads/` — bead graph
- `<audit-dir>/verify.sh` — composite harness
- `<audit-dir>/REVIEWER_RESPONSES.md` — maintainer-empathy review
- `<audit-dir>/AUDIT_SUMMARY.md` — this file

---
name: swarm-exec-status
description: Alias for swarm status; delegates to /swarm-status.
---
# Swarm Exec Status

Alias for swarm status — delegates to `/swarm-status`.

## When to activate

Same triggers as `/swarm-status`:
- "swarm status" / "check swarm" / "swarm progress"
- "how's the swarm" / "agent status"

## Instructions

Read `~/.claude/skills/swarm-status/skill.md` and follow it verbatim. This skill
exists only for naming continuity; the canonical skill is `/swarm-status`. Do
not duplicate its contents here.
